name: todone_lib
description: A library made to parse and write tasks in the ToDone open format.
version: 0.1.0
repository: https://github.com/UriMtzF/todone-lib

environment:
  sdk: ^3.2.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
